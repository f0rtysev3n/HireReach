# HireReach
Server URL, all endpoints' addresses, KV namespace ID, webhook secret handling details, license key format/regex, device cap number, storage key names, the dev-override mechanism, payment page URL, pricing internals, and any file/code structure someone could use to probe or clone the system.
